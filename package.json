{
  "name": "roaring-tigers-api",
  "version": "1.0.0",
  "description": "Backend API for Roaring Tigers CRM",
  "main": "server.js",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build required'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
